# MASTER_ARCHITECTURE — movido a SSOT único

> ⚠️ **Este archivo ya NO es la fuente de verdad.** El MASTER de arquitectura vive en un único
> lugar, y desde el 2026-08-08 ese lugar es el repo **`teseo-bot/micontexto-documents`**:
>
> **SSOT:** `Documents/MASTER_ARCHITECTURE.md`
> **Índice:** `Documents/INDEX.md` — empieza siempre por ahí.
>
> No edites este stub ni recrees una copia local del MASTER. Cualquier cambio de
> arquitectura o despliegue se hace en el SSOT.

## Qué cambió

Este stub apuntaba a `/Users/teseohome/Teseo_AI/docs/MASTER_ARCHITECTURE.md`, ruta **muerta desde
el rename de la raíz del 2026-08-06**. El único puntero cuyo trabajo entero era ser seguido llevaba
dos días roto.

`docs/` pasó a **archivo histórico congelado** el 2026-08-08 (tag `archivo-congelado-2026-08-08`).
La documentación vigente vive ahora en `Documents/`, con frontmatter validado, jerarquía sin
huérfanos, índice compilado y un detector que marca un documento como caducado cuando el código que
cita se mueve.

La decisión original del 2026-07-09 —un solo SSOT, y stubs en los repos de servicio— **sigue
vigente**. Sólo cambia dónde está la casa.
