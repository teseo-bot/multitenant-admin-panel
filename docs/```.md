```mermaid
graph TD
    %% Teseo Internal
    subgraph Teseo_Internal [Infraestructura Teseo]
        A[crm-agentico-panel<br/>Super Admin] -->|Aprovisiona/Gestiona| B[(Supabase Maestro<br/>Tenants & Auth)]
    end

    %% Frontends del Cliente
    subgraph Frontends_Inquilino [Capa de Cliente B2B]
        C[crm-mission-control<br/>Dueño: Configura IA y Prompts] -->|Guarda Config| B
        D[crm-agentico<br/>Vendedor: Atiende Leads y Chats] -->|Lee Datos| B
    end

    %% Capa de Lógica AI
    subgraph Backend_IA [Motores en Cloud Run]
        E[crm-agentico-orchestrator<br/>LangGraph Engine]
        F[crm-agentico-compiler<br/>RAG / Embeddings]
    end

    %% Bases de Datos
    subgraph Data_Layer [Capa de Datos Privada]
        G[(Cloud SQL Privado<br/>pgvector Memoria RAG)]
        H[(pg-boss<br/>Cola de Tareas)]
    end

    %% Relaciones Core
    B -.->|Hidrata Config| E
    C -->|Webhook Emergencias| E
    D <-->|WebSockets/Handoff| E
    E -->|Guarda/Lee Contexto| G
    E -->|Delega Trabajo Pesado| H
    H -->|Consume| F
    F -->|Inyecta Conocimiento| G
    
    %% Tráfico Externo
    WhatsApp/Telegram -->|Webhooks| E
    ```
